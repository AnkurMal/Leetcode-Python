The number of unique paths from the top-left to the bottom-right of an `m x n` grid is given by:

$
\text{Unique Paths} = \binom{m+n-2}{m-1} = \binom{m+n-2}{n-1}
$

Where:

$
\binom{a}{b} = \frac{a!}{b! \cdot (a-b)!}
$

So,

$
\boxed{\text{Unique Paths} = \frac{(m+n-2)!}{(m-1)! \cdot (n-1)!}}
$
